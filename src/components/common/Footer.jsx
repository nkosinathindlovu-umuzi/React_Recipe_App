import styles from "./common.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p>Midlands Table — recipes from Pietermaritzburg's kitchens to yours.</p>
    </footer>
  );
};

export default Footer;
